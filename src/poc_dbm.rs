mod dbm;

fn main() {
    // let result = dbm::read_from_file("./whitelist".to_string(), "ou=users,dc=example,dc=org".to_string());

    // println!("Users: {}", result.whitelisted.iter().map(|e| e.username.to_string()).collect::<Vec<String>>().join(","))
}